import { AppConfig } from "blockstack"

export const appConfig = new AppConfig(['store_write', 'publish_data']) //'store_write' allows you to write information to database, 'publish_data' allows your information to be viewable by other users